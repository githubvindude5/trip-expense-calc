services:
  - type: web
    name: trip-expenses
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
    disk:
      name: trip-data
      mountPath: /data
      sizeGB: 1
